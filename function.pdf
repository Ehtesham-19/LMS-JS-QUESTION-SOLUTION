Here’s a **detailed explanation** of your questions 👇

---

## 1. Self-Invoke / Immediate Invoke Function Expression (IIFE)

### Definition:

A **Self-Invoking Function** (or **IIFE – Immediately Invoked Function Expression**) is a function that runs immediately after it is defined, without being explicitly called elsewhere in the code.

* Purpose:

  * To create a private scope (avoid polluting global scope).
  * To execute code immediately once.

### Syntax (Function Keyword):

```javascript
(function () {
  console.log("This is a self-invoked function!");
})();
```

Here:

* `(function() { ... })` → function expression wrapped in parentheses.
* `()` at the end → executes it immediately.

### Example with Parameters:

```javascript
(function (name) {
  console.log("Hello, " + name);
})("Alice");
```

---

### Syntax (Arrow Function):

```javascript
(() => {
  console.log("This is an IIFE with arrow function!");
})();
```

With parameters:

```javascript
((x, y) => {
  console.log("Sum is: " + (x + y));
})(2, 3);
```

---

## 2. Built-in Functions in JavaScript

### Meaning:

Built-in functions are **predefined functions provided by JavaScript** to perform common tasks without writing extra code.

* Saves time.
* Makes code cleaner and more reliable.
* Increases productivity.

---

### Common **String Built-in Functions**:

1. `length` – returns string length.
2. `charAt(index)` – returns character at given index.
3. `toUpperCase()` – converts string to uppercase.
4. `toLowerCase()` – converts string to lowercase.
5. `trim()` – removes whitespace from both ends.
6. `slice(start, end)` – extracts part of string.
7. `substring(start, end)` – similar to slice but different handling of negative values.
8. `split(separator)` – splits string into array.
9. `replace(old, new)` – replaces part of string.
10. `includes(value)` – checks if substring exists.

---

### Common **Array Built-in Functions**:

1. `push(element)` – add element at end.
2. `pop()` – remove element from end.
3. `shift()` – remove element from beginning.
4. `unshift(element)` – add element at beginning.
5. `concat(array)` – merge arrays.
6. `slice(start, end)` – extract part of array.
7. `splice(start, deleteCount, items...)` – add/remove elements.
8. `indexOf(value)` – get index of element.
9. `includes(value)` – check if element exists.
10. `forEach(callback)` – loop through array.
11. `map(callback)` – return new array by transforming each element.
12. `filter(callback)` – return new array with elements passing condition.
13. `reduce(callback, initialValue)` – reduce array to single value.
14. `find(callback)` – return first matching element.
15. `sort()` – sort elements.
16. `reverse()` – reverse array order.

---

## 3. Benefits of Using Built-in Functions

* **Efficiency**: Saves time, as no need to write custom implementations.
* **Reliability**: Pre-tested and optimized by JavaScript engine.
* **Readability**: Code becomes cleaner and easier to understand.
* **Performance**: Often more optimized than manually written loops.
* **Cross-Browser Support**: Works consistently across environments.

---

👉 Would you like me to **create a PDF** for this explanation (with examples of String and Array built-in functions demonstrated in code), like the previous ones?
