Great set of questions 👌 Let’s go step by step:

---

### **1. Difference between Parameters and Default Parameters**

* **Parameters**
  Parameters are placeholders in a function definition. They accept values (arguments) when the function is called. If no value is passed for a parameter, it will be `undefined` (unless a default value is given).

* **Default Parameters**
  Default parameters provide a **fallback value** when no argument (or `undefined`) is passed for that parameter.

👉 In short:

* **Parameter** → Must be provided, otherwise becomes `undefined`.
* **Default Parameter** → Has a predefined value if not provided.

---

### **2. How to Define a Default Parameter and Can We Override It?**

You define a default parameter by assigning a value inside the function definition:

```javascript
function greet(name = "Guest") {
  return "Hello, " + name;
}

console.log(greet());       // "Hello, Guest"  (default used)
console.log(greet("Alice")); // "Hello, Alice" (overridden)
```

✅ Yes, **you can override** default parameters simply by passing an argument when calling the function.

---

### **3. What is the Data Type of Parameter?**

* Parameters themselves don’t have a fixed data type.
* They take the **data type of the argument** passed.

Example:

```javascript
function checkType(param) {
  console.log(typeof param);
}

checkType(42);          // number
checkType("Hello");     // string
checkType(true);        // boolean
checkType([1, 2, 3]);   // object (array)
checkType({ a: 1 });    // object
```

---

### **4. Function Example with 1 Required & 1 Optional Parameter**

> Requirement: If called with **1 parameter (1)** → return `4`.
> If called with **2 parameters (1,2)** → return `3`.

```javascript
function customFunction(a, b = 3) {
  return a + b;
}

console.log(customFunction(1));    // 1 + 3 = 4
console.log(customFunction(1, 2)); // 1 + 2 = 3
```

✅ Here:

* `a` is a **required parameter**.
* `b` is an **optional parameter with default = 3**.
* If `b` is not provided, it takes the default value `3`.

---

Would you like me to also **make a PDF** for this explanation (like we did with your earlier assignments) so you can keep all these function-related topics together?
